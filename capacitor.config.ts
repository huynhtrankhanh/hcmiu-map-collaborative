import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.huynhtrankhanh.hcmiumap',
  appName: 'HCMIU Map',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
